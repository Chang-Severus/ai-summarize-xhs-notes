{
  "_说明": "复制本文件为 config.json 后填写。多收藏夹：在 collections 里加项；运行脚本用 --collection <id> 指定。",

  "_collections说明": "你的收藏夹列表，每个独立隔离。board_url 从小红书网页版打开收藏夹后地址栏复制。新增收藏夹复制一项改 id/name/board_url 即可。",
  "collections": [
    {
      "id": "ai",
      "name": "AI / LLM / Agent 技术",
      "board_url": "https://www.xiaohongshu.com/board/你的收藏夹ID?...",
      "max_notes": 200,
      "_max_notes说明": "该收藏夹最多抓多少条，防止一次抓太多触发风控"
    }
  ],

  "_全局抓取参数说明": "所有收藏夹共用",
  "scroll_pause_seconds": 2.5,
  "_scroll_pause说明": "每次滚动加载后的等待秒数，越大越安全（越像真人）",

  "note_interval_seconds": 4,
  "_note_interval说明": "抓完一条帖子后等待秒数，控制频率防风控",

  "download_images": true,
  "_download_images说明": "是否下载帖子图片用于OCR。图文型干货建议开启",

  "headless": false,
  "_headless说明": "false=显示浏览器窗口（首次登录需要）。抓取时建议保持false便于观察",

  "_图片理解说明": "图片里干货的提取引擎。provider=rapidocr 为本地免费OCR；provider=vlm 为多模态大模型（效果更好，需填 base_url/model/api_key）。想切换只改这里，无需改代码。",
  "image_understanding": {
    "provider": "rapidocr",

    "_rapidocr参数": "本地免费OCR的可选参数",
    "min_score": 0.5,

    "_vlm参数说明": "当 provider 改成 vlm 时使用。已默认填好公司 venus 代理 + qwen-vl 视觉模型。注意：model 必须是支持视觉/多模态的模型，纯文本模型（如 qwen-14b-chat）无法读图！",
    "base_url": "http://v2.open.venus.oa.com/llmproxy",
    "model": "qwen-vl-max",
    "api_key": "在这里填你的 venus API Key",
    "detail": "high",
    "timeout": 60,
    "max_retries": 2,
    "concurrency": 10,
    "_concurrency备注": "图片提取并发数（仅对 vlm 生效，rapidocr 自动串行）。默认10，调大更快但可能触发API限流。",

    "_其它厂商示例": {
      "通义千问Qwen-VL": { "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1", "model": "qwen-vl-max 或 qwen-vl-plus" },
      "OpenAI": { "base_url": "https://api.openai.com/v1", "model": "gpt-4o 或 gpt-4o-mini" }
    }
  },

  "_总结说明": "知识点总结。默认 enabled=false：推荐直接在对话里让 Claude 帮你总结（质量最高、0成本）。若想用 API 全自动跑，设 enabled=true 并填好 model/api_key，再运行 python scraper/summarize.py。推荐总结用 Claude Sonnet。",
  "summarization": {
    "enabled": false,
    "base_url": "http://v2.open.venus.oa.com/llmproxy",
    "model": "claude-sonnet（以 venus 实际模型名为准）",
    "api_key": "在这里填你的 venus API Key",
    "batch_size": 8,
    "temperature": 0,
    "timeout": 120,
    "max_retries": 2
  },

  "_发布说明": "把网页自动推送到 GitHub Pages。enabled=true 开启；auto=true 表示总结完自动推送。repo_url 填你的仓库地址（推荐 SSH 格式 git@github.com:用户名/仓库名.git）。",
  "publish": {
    "enabled": false,
    "auto": true,
    "repo_url": "git@github.com:用户名/仓库名.git",
    "branch": "main"
  }
}
